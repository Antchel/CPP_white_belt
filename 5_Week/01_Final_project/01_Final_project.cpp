#include "Database.h"
#include "Date.h"
#include <string>
#include <iostream>

using namespace std;

int main() {
    Database db;

    string command;
    while (getline(cin, command)) {
        // read commands from stream and processed them
    }

    return 0;
}